import React from 'react';
import UserList from '@/views/apps/user/list';

export const metadata = {
  title: "User List - Meschino Health",
};

const page = () => {
  return <UserList />
}

export default page;
