import React from 'react';
import AddUserDrawer from '@/views/apps/user/add/AddUserDrawer';

export const metadata = {
  title: "Add User - Meschino Health",
};

const page = () => {
  return <AddUserDrawer />
}

export default page;
