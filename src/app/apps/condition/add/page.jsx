import React from 'react';
import AddConditionDrawer from '@/views/apps/condition/add/AddConditionDrawer';

export const metadata = {
  title: "Add Condition - Health Conditions",
};

const page = () => {
  return <AddConditionDrawer />
}

export default page;
