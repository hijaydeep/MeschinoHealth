import React from 'react';
import ConditionList from '@/views/apps/condition/list';

export const metadata = {
  title: "Condition List - Health Conditions",
};

const page = () => {
  return <ConditionList />
}

export default page;
